FAILURE_OAUTH_BASE_URL=http://localhost:3000
FAILURE_OAUTH_ISSUER=http://localhost:3000
FAILURE_SESSION_SECRET=dev-failure-session-secret-change-me
FAILURE_ENCRYPTION_KEY=dev-failure-encryption-key-change-me

# Production (Cloudflare Workers / oauth.failure.fail):
# FAILURE_OAUTH_BASE_URL and FAILURE_OAUTH_ISSUER are set in wrangler.jsonc
# FAILURE_SESSION_SECRET and FAILURE_ENCRYPTION_KEY are Wrangler secrets
# Storage uses Cloudflare KV binding FAILURE_KV
